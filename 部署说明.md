# 植物识别系统部署说明

## 一、系统要求

### 1. 硬件要求
- **CPU**: 支持AVX指令集的多核处理器（推荐4核以上）
- **内存**: 最低8GB，推荐16GB以上
- **存储**: 至少10GB可用空间（模型文件较大）
- **GPU**: 可选，支持CUDA的NVIDIA显卡可加速识别

### 2. 操作系统
- Windows 10/11 (64位)
- Linux (Ubuntu 18.04+)
- macOS (Intel/Apple Silicon)

---

## 二、软件环境要求

### 1. Python环境
- **Python版本**: 3.8 - 3.11（推荐3.10）
- **pip版本**: 21.0+

### 2. JDK要求（Android开发）
- **JDK版本**: JDK 11 或 JDK 17（推荐JDK 17）
- **Android Studio**: Arctic Fox (2020.3.1) 或更高版本
- **Android SDK**: API Level 26+ (Android 8.0+)

### 3. OpenCV要求
项目使用了OpenCV进行图像预处理，需要安装OpenCV。

#### 方法一：pip安装（推荐）
```bash
# Windows/Linux/macOS
pip install opencv-python>=4.8.0

# 或者使用conda
conda install -c conda-forge opencv
```

#### 方法二：手动安装OpenCV SDK（如果pip安装失败）

**Windows手动安装步骤：**

1. **下载OpenCV**
   - 访问官网：https://opencv.org/releases/
   - 下载 Windows 版本（如 `opencv-4.8.1-windows.exe`）
   - 运行安装程序，选择解压目录（如 `C:\opencv`）

2. **配置环境变量**
   ```
   右键"此电脑" -> 属性 -> 高级系统设置 -> 环境变量
   
   在"系统变量"中找到 Path，添加以下路径：
   C:\opencv\build\x64\vc16\bin
   C:\opencv\build\x64\vc16\lib
   ```

3. **验证安装**
   ```bash
   python -c "import cv2; print(cv2.__version__)"
   # 应输出: 4.8.1
   ```

**Linux手动安装步骤：**

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y libopencv-dev python3-opencv

# 或者从源码编译安装
sudo apt-get install -y build-essential cmake git libgtk-3-dev \
    libavcodec-dev libavformat-dev libswscale-dev libv4l-dev \
    libxvidcore-dev libx264-dev libdc1394-dev libgstreamer-plugins-base1.0-dev \
    libgstreamer1.0-dev libtbb2 libtbb-dev libjpeg-dev libpng-dev libtiff-dev

# 下载源码
cd ~
git clone https://github.com/opencv/opencv.git
cd opencv
git checkout 4.8.1

# 编译安装
mkdir build && cd build
cmake -D CMAKE_BUILD_TYPE=RELEASE \
      -D CMAKE_INSTALL_PREFIX=/usr/local \
      -D INSTALL_PYTHON_EXAMPLES=ON \
      -D INSTALL_C_EXAMPLES=OFF \
      -D OPENCV_ENABLE_NONFREE=ON \
      -D WITH_CUDA=ON ..  # 如果需要CUDA支持

make -j$(nproc)
sudo make install
sudo ldconfig
```

**macOS手动安装步骤：**

```bash
# 使用Homebrew安装
brew install opencv

# 或者使用pip
pip install opencv-python

# 验证安装
python -c "import cv2; print(cv2.__version__)"
```

#### 方法三：使用预编译的whl文件（Windows离线安装）

如果网络受限，可以下载预编译的whl文件：

1. **下载whl文件**
   - 访问：https://www.lfd.uci.edu/~gohlke/pythonlibs/#opencv
   - 根据Python版本选择对应的文件：
     - Python 3.10: `opencv_python-4.8.1-cp310-cp310-win_amd64.whl`
     - Python 3.11: `opencv_python-4.8.1-cp311-cp311-win_amd64.whl`

2. **安装whl文件**
   ```bash
   # 进入下载目录
   cd Downloads
   
   # 安装
   pip install opencv_python-4.8.1-cp310-cp310-win_amd64.whl
   ```

#### 常见问题

**问题1: `ImportError: DLL load failed`**
```bash
# 解决：安装Visual C++ Redistributable
# 下载地址：https://aka.ms/vs/17/release/vc_redist.x64.exe
```

**问题2: `ModuleNotFoundError: No module named 'cv2'`**
```bash
# 解决：检查Python环境，确保在正确的环境中安装
which python
python -m pip install opencv-python
```

**问题3: 找不到cv2模块的特定功能**
```bash
# 解决：安装完整版opencv-contrib
pip install opencv-contrib-python
```

---

## 三、后端部署步骤

### 1. 创建虚拟环境

```bash
# 进入后端目录
cd backEnd

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate
```

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

**注意**: requirements.txt 需要更新以下依赖（当前版本可能缺失）：

```
# 在 requirements.txt 中添加以下依赖
opencv-python>=4.8.0
pytz>=2023.3
```

### 3. 模型文件准备

确保以下模型文件存在于 `backEnd/models/` 目录：
- `efficientnet_cbam_plants.pth` - EfficientNet+CBAM分类模型
- `yolov10n_plant_detection.pt` - YOLOv10检测模型

### 4. 启动服务

```bash
# 开发模式（带热重载）
python -m uvicorn main:app --host 0.0.0.0 --port 8002 --reload

# 生产模式
python main.py
```

服务启动后会显示：
```
✅ 服务启动成功: http://0.0.0.0:8002
```

---

## 四、Android端部署步骤

### 1. 环境配置

1. 安装 **JDK 17**（Oracle JDK 或 OpenJDK）
2. 安装 **Android Studio** 最新稳定版
3. 在 Android Studio 中配置 SDK：
   - SDK Platforms: Android 13.0 (API 33)
   - SDK Tools: Android SDK Build-Tools 33+

### 2. OpenCV SDK配置（Android端）

项目已集成OpenCV Android SDK，位于 `android/opencv` 目录。如果重新安装或更新OpenCV SDK，请按以下步骤操作：

#### 方法一：使用项目中已有的OpenCV SDK（推荐）

项目中已包含OpenCV 4.8.1 SDK，位于 `android/opencv` 目录，无需额外配置。

**验证OpenCV配置：**
1. 检查 `android/settings.gradle` 中是否包含：
   ```gradle
   include ':opencv'
   ```

2. 检查 `android/app/build.gradle` 中是否包含：
   ```gradle
   dependencies {
       ...
       implementation project(':opencv')
   }
   ```

#### 方法二：手动导入OpenCV Android SDK

如果需要更新或重新安装OpenCV SDK：

**步骤1：下载OpenCV Android SDK**

1. 访问OpenCV官网：https://opencv.org/releases/
2. 下载 **Android** 版本（如 `opencv-4.8.1-android-sdk.zip`）
3. 解压到项目目录：
   ```
   android/
   ├── app/
   ├── opencv/          # 解压后的OpenCV SDK
   │   ├── build.gradle
   │   ├── java/
   │   ├── native/
   │   └── etc/
   └── settings.gradle
   ```

**步骤2：导入OpenCV模块**

1. 打开 Android Studio
2. 选择 **File** -> **New** -> **Import Module**
3. 选择解压后的OpenCV SDK目录中的 `sdk` 文件夹
4. 模块名称设置为 `opencv`
5. 点击 **Finish**

**步骤3：添加依赖**

在 `android/app/build.gradle` 中添加：
```gradle
dependencies {
    ...
    implementation project(':opencv')
}
```

**步骤4：同步项目**

点击 **Sync Now** 同步Gradle配置。

#### 方法三：使用OpenCV Manager（不推荐）

旧版本使用OpenCV Manager，但此方法已废弃，建议使用上述静态库方式。

#### 常见问题

**问题1: `Could not find :opencv:`**

**解决**：
1. 检查 `settings.gradle` 中是否正确包含OpenCV模块路径
2. 检查OpenCV目录结构是否正确
3. 清理并重建项目：
   ```
   Build -> Clean Project
   Build -> Rebuild Project
   ```

**问题2: `UnsatisfiedLinkError: dlopen failed: library "libopencv_java4.so" not found`**

**解决**：
1. 检查 `android/app/build.gradle` 中的 `packagingOptions`：
   ```gradle
   packagingOptions {
       pickFirst 'lib/arm64-v8a/libc++_shared.so'
       pickFirst 'lib/armeabi-v7a/libc++_shared.so'
       pickFirst 'lib/x86/libc++_shared.so'
       pickFirst 'lib/x86_64/libc++_shared.so'
   }
   ```

2. 确保OpenCV的native库被正确打包

**问题3: OpenCV版本兼容性**

**解决**：
- 当前项目使用OpenCV 4.8.1
- 如需升级，请同时更新：
  1. OpenCV SDK
  2. 相关Java代码中的API调用
  3. 重新编译测试

### 3. 项目配置

1. 打开 Android Studio
2. 选择 "Open an existing Android Studio project"
3. 选择 `android` 文件夹
4. 等待 Gradle 同步完成（首次同步可能需要下载依赖，时间较长）

### 3. 修改服务器地址

在 `android/app/src/main/java/com/plantrecognition/app/network/ApiClient.java` 中：

```java
// 修改为实际的服务器地址
private static final String BASE_URL = "http://192.168.10.3:8002/api/v1/";
```

### 4. 构建APK

```bash
# 通过 Android Studio
Build -> Build Bundle(s) / APK(s) -> Build APK(s)

# 或通过命令行
./gradlew assembleDebug
```

---

## 五、依赖版本检查清单

### 后端关键依赖版本要求

| 依赖包 | 当前要求 | 推荐版本 | 说明 |
|--------|----------|----------|------|
| Python | 3.8+ | 3.10.x | 必须 |
| FastAPI | >=0.104.0 | 0.104.1 | Web框架 |
| uvicorn | >=0.24.0 | 0.24.0 | ASGI服务器 |
| SQLAlchemy | >=2.0.0 | 2.0.23 | ORM |
| PyTorch | >=2.0.0 | 2.1.0 | 深度学习框架 |
| torchvision | >=0.15.0 | 0.16.0 | 视觉工具包 |
| NumPy | >=1.24.0 | 1.24.3 | 数值计算 |
| Pillow | >=10.0.0 | 10.1.0 | 图像处理 |
| OpenCV | 未指定 | >=4.8.0 | **需要添加** |
| pytz | 未指定 | >=2023.3 | **需要添加** |
| pydantic | >=2.0.0 | 2.5.0 | 数据验证 |
| python-multipart | >=0.0.6 | 0.0.6 | 文件上传 |

### 检查依赖版本命令

```bash
# 检查已安装版本
pip list | grep -E "(fastapi|uvicorn|torch|opencv|pytz|sqlalchemy)"

# 检查是否有冲突
pip check
```

---

## 六、常见问题解决

### 1. OpenCV安装失败

**问题**: `pip install opencv-python` 失败

**解决**:
```bash
# 方法1: 使用清华镜像
pip install opencv-python -i https://pypi.tuna.tsinghua.edu.cn/simple

# 方法2: 下载whl文件手动安装
# 访问 https://www.lfd.uci.edu/~gohlke/pythonlibs/#opencv
# 下载对应Python版本的whl文件
pip install opencv_python-4.8.1-cp310-cp310-win_amd64.whl
```

### 2. PyTorch CUDA版本不匹配

**问题**: GPU加速无法使用

**解决**:
```bash
# 卸载当前版本
pip uninstall torch torchvision

# 安装CUDA 11.8版本
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# 或CPU版本
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
```

### 3. pytz模块缺失

**问题**: `ModuleNotFoundError: No module named 'pytz'`

**解决**:
```bash
pip install pytz
```

### 4. Android编译失败

**问题**: Gradle同步失败或编译错误

**解决**:
1. 检查 `gradle/wrapper/gradle-wrapper.properties` 中的Gradle版本
2. 检查 `build.gradle` 中的AGP版本与Android Studio兼容
3. 清理缓存: `File -> Invalidate Caches / Restart`

---

## 七、更新requirements.txt

请将以下内容添加到 `backEnd/requirements.txt`：

```
# 图像处理
opencv-python>=4.8.0

# 时区处理
pytz>=2023.3

# 其他可能缺失的依赖
matplotlib>=3.7.0
tqdm>=4.65.0
```

然后执行：
```bash
pip install -r requirements.txt --upgrade
```

---

## 八、验证部署

### 后端验证

1. 访问 Swagger 文档：`http://localhost:8002/docs`
2. 测试识别接口：使用 `/recognize` 接口上传图片
3. 检查日志输出：确认模型加载成功

### Android验证

1. 安装APK到设备
2. 测试登录/注册功能
3. 测试拍照识别功能
4. 检查图片是否正常显示

---

## 九、生产环境部署建议

1. **使用Gunicorn+Uvicorn**:
   ```bash
   pip install gunicorn
   gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8002
   ```

2. **使用Nginx反向代理**:
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       
       location / {
           proxy_pass http://127.0.0.1:8002;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

3. **使用Docker部署**（可选）:
   ```dockerfile
   FROM python:3.10-slim
   WORKDIR /app
   COPY requirements.txt .
   RUN pip install -r requirements.txt
   COPY . .
   CMD ["python", "main.py"]
   ```

---

**部署日期**: 2026-03-15  
**版本**: v1.0
