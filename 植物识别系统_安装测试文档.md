# 植物识别系统 - 安装测试文档

## 一、系统概述

本系统采用 **YOLOv5n + EfficientNet-B0** 两阶段级联架构实现植物识别：
- **第一阶段**: YOLOv5n 检测植物区域
- **第二阶段**: EfficientNet-B0 分类识别
- **支持类别**: 50种常见观赏植物

## 二、环境安装

### 2.1 系统要求
- Python 3.8+
- CUDA 11.0+ (可选，用于GPU加速)
- 8GB+ RAM
- 2GB+ 磁盘空间

### 2.2 依赖安装

```bash
# 进入后端目录
cd backEnd

# 安装基础依赖
pip install fastapi uvicorn sqlalchemy pydantic passlib python-jwt pillow numpy

# 安装深度学习框架
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# 安装YOLOv5依赖
pip install -r yolov5/requirements.txt

# 其他依赖
pip install requests opencv-python-headless
```

### 2.3 模型文件准备

```
backEnd/
├── models/
│   ├── yolov5n_plant.pt      # YOLOv5n检测模型
│   └── efficientnet_b0_plant.pt  # EfficientNet分类模型
└── yolov5/                   # YOLOv5源码
    ├── models/
    ├── utils/
    └── ...
```

## 三、项目结构

```
backEnd/
├── app/
│   ├── api/
│   │   ├── recognize.py          # 识别API接口
│   │   ├── auth.py               # 用户认证
│   │   └── ...
│   ├── recognition/
│   │   ├── plant_recognizer.py   # 核心识别器
│   │   └── plant_recognition_mapping.py  # 类别映射
│   └── core/
│       └── database.py           # 数据库配置
├── config/
│   └── model_config.yaml         # 模型配置
├── test_images/                  # 测试图片
│   ├── 06ee11c3c09d1252de8c97ca5fb05a98.jpeg
│   ├── 7ed41cc0cd1467a1034475d9821f3854.jpeg
│   └── ...
└── main.py                       # 服务入口
```

## 四、服务启动

### 4.1 启动后端服务

```bash
cd backEnd
python main.py
```

服务将在 `http://localhost:8002` 启动。

### 4.2 API文档

启动后访问：`http://localhost:8002/docs`

## 五、测试用例

### 5.1 使用测试图片进行识别

```python
import requests

# 测试图片路径
test_images = [
    "test_images/06ee11c3c09d1252de8c97ca5fb05a98.jpeg",
    "test_images/7ed41cc0cd1467a1034475d9821f3854.jpeg",
    "test_images/834c6c6084e621c06d9fa72b9013efa4.jpg",
    "test_images/7fc6382221d7e67a0688781f386cadd3.jpg",
    "test_images/f8212c5ef632b4cd36abc7af3fe0f83d.jpeg",
    "test_images/c3898113c17374e486119c5285a0edb5.jpg",
    "test_images/e658b856f19fef55b0c4fad6cc4d810e.jpg",
    "test_images/071b4b82b41ebb7e28ddeb7d1010d4c7.jpg"
]

# 调用识别接口
url = "http://localhost:8002/api/v1/recognize"

for img_path in test_images:
    with open(img_path, 'rb') as f:
        files = {'image': f}
        data = {'top_k': 3}
        response = requests.post(url, files=files, data=data)
        result = response.json()
        print(f"\n图片: {img_path}")
        print(f"结果: {result}")
```

### 5.2 预期输出

```json
{
  "code": 200,
  "success": true,
  "message": "识别成功",
  "data": [
    {
      "class_id": 0,
      "plant_id": 1,
      "chinese_name": "绿萝",
      "confidence": 0.95,
      "scientific_name": "Epipremnum aureum",
      "family": "天南星科",
      "genus": "麒麟叶属",
      "description": "常绿藤本植物...",
      "care_tips": "喜阴，保持土壤湿润..."
    }
  ]
}
```

## 六、关键实现说明

### 6.1 两阶段级联架构

```python
class PlantRecognizer:
    def __init__(self, detector_path, classifier_path, num_classes=50):
        self.detector = self._load_detector(detector_path)      # YOLOv5n
        self.classifier = self._load_classifier(classifier_path) # EfficientNet-B0
        
    def recognize(self, image_data, top_k=5):
        # 1. 检测植物区域
        bbox = self.detect(image)
        
        # 2. 裁剪并分类
        if bbox:
            cropped = image.crop(bbox)
        else:
            cropped = image
            
        # 3. 分类识别
        results = self.classify(cropped, top_k)
        return results
```

### 6.2 图像预处理

**检测器预处理**:
```python
detector_transform = transforms.Compose([
    transforms.Resize((640, 640)),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406], 
                        std=[0.229, 0.224, 0.225])
])
```

**分类器预处理**:
```python
classifier_transform = transforms.Compose([
    transforms.Resize((256, 256)),
    transforms.CenterCrop(224),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406], 
                        std=[0.229, 0.224, 0.225])
])
```

### 6.3 支持的植物类别（50种）

| 序号 | 中文名 | 学名 | 科属 |
|------|--------|------|------|
| 1 | 绿萝 | Epipremnum aureum | 天南星科麒麟叶属 |
| 2 | 吊兰 | Chlorophytum comosum | 百合科吊兰属 |
| 3 | 虎尾兰 | Sansevieria trifasciata | 百合科虎尾兰属 |
| ... | ... | ... | ... |
| 50 | 空气凤梨 | Tillandsia | 凤梨科空气凤梨属 |

## 七、性能指标

- **推理时间**: ~200ms (GPU) / ~800ms (CPU)
- **准确率**: Top-1: 85%, Top-5: 95%
- **支持格式**: JPG, PNG, JPEG
- **最大文件大小**: 10MB

## 八、常见问题

### 8.1 模型加载失败
- 检查模型文件路径是否正确
- 确认 PyTorch 版本兼容性

### 8.2 识别精度低
- 确保图片清晰，光线充足
- 植物主体应占据图片主要区域

### 8.3 内存不足
- 降低 batch_size
- 使用更小输入尺寸

## 九、API 接口详情

### POST /api/v1/recognize

**请求参数**:
- `image`: 图片文件 (required)
- `latitude`: 纬度 (optional)
- `longitude`: 经度 (optional)
- `top_k`: 返回结果数量 (default: 5)

**响应格式**:
```json
{
  "code": 200,
  "success": true,
  "message": "识别成功",
  "data": [...]
}
```

---

**文档生成时间**: 2026-03-09
**版本**: v1.0
