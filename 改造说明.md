# 植物识别系统改造说明

## 一、改造概述
haha

本次改造将原有的毒性植物识别系统改造为**单一植物实时识别系统**，采用设计文档中的两阶段级联架构（YOLOv5n检测 + EfficientNet分类）。

## 二、主要改造内容

### 1. 数据库改造

**文件**: `backEnd/database/plant_recognition.sqlite.sql`

- 使用SQLite替代MySQL
- 移除外键约束，通过应用层维护关联
- 所有字段添加中文注释
- 包含50种常见观赏植物数据（无毒性信息）

**植物分类**:
- 室内观叶植物（15种）: 绿萝、吊兰、虎尾兰、龟背竹、发财树等
- 开花观赏植物（20种）: 月季、茉莉、栀子、杜鹃、蝴蝶兰等
- 多肉植物（10种）: 芦荟、仙人掌、玉露、熊童子等
- 水培/香草植物（5种）: 薄荷、迷迭香、薰衣草、铁线蕨、空气凤梨

**数据库表结构**:
```sql
plants表 - 植物信息表
  - id: 植物ID（主键）
  - class_id: 模型类别ID（0-49）
  - chinese_name: 中文名称
  - scientific_name: 学名
  - family/genus: 科/属
  - description: 植物描述
  - characteristics: 形态特征
  - flowering_period: 花期
  - care_tips: 养护建议
  - difficulty_level: 养护难度（1简单/2中等/3困难）

recognition_history表 - 识别历史表
  - id: 记录ID
  - plant_id/class_id: 关联植物ID/类别ID
  - confidence: 置信度
  - plant_name: 植物名称
  - image_path: 图片路径
  - latitude/longitude: 位置信息
  - user_id: 用户ID
  - is_favorite: 是否收藏
  - created_at: 识别时间
```

### 2. 后端识别服务改造

**新文件**:
- `backEnd/app/recognition/plant_recognizer.py` - 两阶段识别器
- `backEnd/app/models/database.py` - 数据库模型
- `backEnd/app/core/database.py` - 数据库连接管理
- `backEnd/app/api/recognize.py` - 识别API（更新）
- `backEnd/app/api/history.py` - 历史记录API
- `backEnd/app/api/flowers.py` - 植物信息API
- `backEnd/main.py` - 主程序（更新）

**识别流程**:
```
输入图像 → YOLOv5n检测植物区域 → 裁剪ROI → EfficientNet分类 → 返回Top-5结果
```

**API接口**:
- `POST /api/v1/recognize` - 识别植物图片
- `GET /api/v1/plants` - 获取植物列表
- `GET /api/v1/plants/{id}` - 获取植物详情
- `GET /api/v1/families` - 获取科分类列表
- `GET /api/v1/history/list` - 获取历史记录
- `POST /api/v1/history/save` - 保存识别历史
- `POST /api/v1/history/{id}/favorite` - 切换收藏状态

### 3. Android界面改造

**改造文件**:
- `android/app/src/main/res/layout/activity_result.xml` - 结果页面布局
- `android/app/src/main/java/com/example/plantrecognition/ui/result/ResultActivity.java` - 结果Activity
- `android/app/src/main/java/com/example/plantrecognition/network/RecognitionResponse.java` - 数据模型
- `android/app/src/main/java/com/example/plantrecognition/network/ApiService.java` - API接口

**新增文件**:
- `android/app/src/main/res/layout/item_similar_plant.xml` - 相似推荐项布局

**UI变更**:
- ❌ 移除了毒性相关UI（毒性等级、有毒部位、中毒症状、急救建议）
- ✅ 新增了养护难度显示
- ✅ 新增了相似推荐列表（Top 2-5）
- ✅ 优化了百科科普和养护指南展示
- ✅ 添加了科属标签显示

## 三、文件结构

```
d:\project\danyizhiwushibie\
├── backEnd/
│   ├── app/
│   │   ├── api/
│   │   │   ├── __init__.py
│   │   │   ├── recognize.py      # 识别API
│   │   │   ├── history.py        # 历史记录API
│   │   │   └── flowers.py        # 植物信息API
│   │   ├── core/
│   │   │   ├── __init__.py
│   │   │   ├── config.py         # 配置加载
│   │   │   └── database.py       # 数据库管理
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   └── database.py       # 数据模型
│   │   └── recognition/
│   │       ├── __init__.py
│   │       └── plant_recognizer.py  # 两阶段识别器
│   ├── database/
│   │   └── plant_recognition.sqlite.sql  # 数据库初始化脚本
│   ├── main.py                   # 主程序
│   └── requirements.txt          # 依赖
├── android/
│   └── app/src/main/
│       ├── java/com/example/plantrecognition/
│       │   ├── network/
│       │   │   ├── ApiService.java
│       │   │   └── RecognitionResponse.java
│       │   └── ui/result/
│       │       └── ResultActivity.java
│       └── res/layout/
│           ├── activity_result.xml
│           └── item_similar_plant.xml
└── 改造说明.md
```

## 四、使用说明

### 1. 初始化数据库

```bash
cd backEnd
python -c "from app.core.database import init_db; init_db()"
```

### 2. 安装依赖

```bash
cd backEnd
pip install -r requirements.txt
```

### 3. 启动服务

```bash
cd backEnd
python main.py
```

### 4. 模型准备

需要准备以下模型文件：
- `models/yolov5n_plant.pt` - YOLOv5n检测模型
- `models/efficientnet_b0_plant.pt` - EfficientNet分类模型

如果模型不存在，系统会使用ImageNet预训练权重进行特征提取。

## 五、API示例

### 识别植物
```bash
curl -X POST "http://localhost:8000/api/v1/recognize" \
  -F "image=@test.jpg" \
  -F "top_k=5"
```

响应：
```json
{
  "code": 200,
  "success": true,
  "message": "识别成功",
  "data": [
    {
      "class_id": 0,
      "plant_id": 1,
      "chinese_name": "绿萝",
      "scientific_name": "Epipremnum aureum",
      "confidence": 0.9567,
      "family": "天南星科",
      "genus": "麒麟叶属",
      "description": "绿萝是常见的室内观叶植物...",
      "care_tips": "喜散射光，忌强光直射...",
      "difficulty_level": 1
    }
  ]
}
```

## 六、注意事项

1. **模型训练**: 当前使用的是ImageNet预训练权重，如需更好的识别效果，需要使用植物数据集进行微调训练
2. **YOLOv5依赖**: 如需使用检测功能，需要安装YOLOv5相关依赖或将其放入项目目录
3. **SQLite**: 数据库文件默认存储在 `backEnd/database/plant_recognition.db`
4. **图片存储**: 识别历史中的图片路径需要应用层管理，建议统一存储在特定目录
